/**
 * Protocol crypto primitives. Pure math layer.
 * Zero dependency on errors, vault, or runtime. Node crypto + buffer only.
 */
export interface KeyPair {
    publicKey?: string;
    privateKey: string;
}
export declare function generateIdentityKeys(): KeyPair;
export declare function signPayload(privateKey: string, payload: string): string;
export declare function derivePublicKey(privateKey: string): string;
export declare function verifySignature(publicKey: string, signature: string, nonce: string): boolean;
export declare function generateNonce(): string;
